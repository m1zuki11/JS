let arr = ['a', 'b', 'c'];
console.log(arr.length) //выводит количество переменных в массиве независимо от типа данных

let arr1 = ['a', 'b', 'c'];
console.log(arr1[arr1.length - 1]) //выводит последний символ из массива 